# Руководство по публикации пакета в npm

## Подготовка к публикации

### 1. Проверка конфигурации

Убедитесь, что в `package.json` правильно настроены:

- `name`: `@moseffect21/appmetrica-push-sdk`
- `version`: актуальная версия
- `main`: `src/index.ts`
- `types`: `src/index.ts`
- `files`: включает все необходимые файлы

### 2. Подготовка пакета

```bash
# Установка зависимостей
npm install

# Проверка линтера
npm run lint
```

### 3. Тестирование

```bash
# Запуск тестов
npm test
```

## Публикация в npm

### 🚀 Автоматическая публикация (рекомендуется)

Используйте готовые скрипты для автоматической публикации:

#### Linux/macOS:

```bash
# Patch версия (1.0.0 -> 1.0.1)
./scripts/publish.sh patch

# Minor версия (1.0.0 -> 1.1.0)
./scripts/publish.sh minor

# Major версия (1.0.0 -> 2.0.0)
./scripts/publish.sh major
```

#### Windows:

```cmd
# Patch версия (1.0.0 -> 1.0.1)
scripts\publish.bat patch

# Minor версия (1.0.0 -> 1.1.0)
scripts\publish.bat minor

# Major версия (1.0.0 -> 2.0.0)
scripts\publish.bat major
```

#### Через npm scripts:

```bash
# Patch версия
npm run publish:patch

# Minor версия
npm run publish:minor

# Major версия
npm run publish:major

# Проверка содержимого пакета
npm run publish:check
```

### 📋 Что делают скрипты:

1. ✅ Проверяют авторизацию в npm
2. ✅ Устанавливают зависимости
3. ✅ Запускают линтер
4. ✅ Запускают тесты (если есть)
5. ✅ Проверяют содержимое пакета
6. ✅ Обновляют версию
7. ✅ Создают архив для проверки
8. ✅ Запрашивают подтверждение
9. ✅ Публикуют пакет
10. ✅ Создают git тег и коммит
11. ✅ Отправляют изменения в GitHub
12. ✅ Автоматически создают GitHub Release

### 🔧 Ручная публикация

Если нужно выполнить публикацию вручную:

#### 1. Авторизация в npm

```bash
# Вход в npm (если не авторизованы)
npm login

# Проверка текущего пользователя
npm whoami
```

#### 2. Проверка пакета перед публикацией

```bash
# Проверка содержимого пакета
npm pack --dry-run

# Проверка, что пакет можно установить
npm pack
tar -tzf moseffect21-appmetrica-push-sdk-1.0.0.tgz
```

#### 3. Публикация

```bash
# Публикация пакета
npm publish --access public
```

> **Важно**: Используйте `--access public` для scoped пакетов (начинающихся с `@`)

### 4. Проверка публикации

```bash
# Проверка, что пакет опубликован
npm view @moseffect21/appmetrica-push-sdk

# Тестовая установка
npm install @moseffect21/appmetrica-push-sdk@latest
```

## Обновление версии

### 1. Обновление версии

```bash
# Patch версия (1.0.0 -> 1.0.1)
npm version patch

# Minor версия (1.0.0 -> 1.1.0)
npm version minor

# Major версия (1.0.0 -> 2.0.0)
npm version major
```

### 2. Публикация обновления

```bash
# Проверка и публикация
npm run lint
npm publish --access public

# Создание git тега и отправка в GitHub
git add package.json
git commit -m "chore: bump version to X.X.X"
git tag "vX.X.X"
git push origin main
git push origin "vX.X.X"
```

## Установка пользователями

После публикации пользователи смогут установить пакет:

```bash
# Через npm
npm install @moseffect21/appmetrica-push-sdk

# Через yarn
yarn add @moseffect21/appmetrica-push-sdk

# Через pnpm
pnpm add @moseffect21/appmetrica-push-sdk
```

## Troubleshooting

### Ошибка "Package name already exists"

Если пакет с таким именем уже существует:

1. Измените имя в `package.json`
2. Или используйте другую версию

### Ошибка "You do not have permission"

1. Убедитесь, что вы авторизованы: `npm whoami`
2. Проверьте, что у вас есть права на публикацию в scope `@moseffect21`

### Ошибка "Invalid package name"

1. Проверьте, что имя пакета соответствует требованиям npm
2. Scoped пакеты должны быть в формате `@scope/package-name`

## GitHub Releases

### Автоматическое создание Release

При публикации пакета автоматически создается GitHub Release:

1. **Git тег** - создается тег `v1.0.0`
2. **GitHub Release** - автоматически создается через GitHub Actions
3. **Changelog** - генерируется из git коммитов
4. **Документация** - включаются ссылки на документацию

### Просмотр релизов

- **GitHub Releases**: https://github.com/moseffect21/appmetrica-push-sdk/releases
- **npm package**: https://www.npmjs.com/package/@moseffect21/appmetrica-push-sdk

### Ручное создание Release

Если нужно создать Release вручную:

1. Перейдите в GitHub → Releases
2. Нажмите "Create a new release"
3. Выберите тег `v1.0.0`
4. Заполните описание
5. Опубликуйте

## Полезные команды

```bash
# Просмотр информации о пакете
npm view @moseffect21/appmetrica-push-sdk

# Просмотр версий пакета
npm view @moseffect21/appmetrica-push-sdk versions --json

# Удаление пакета (в течение 24 часов после публикации)
npm unpublish @moseffect21/appmetrica-push-sdk@1.0.0

# Просмотр статистики загрузок
npm view @moseffect21/appmetrica-push-sdk downloads

# Просмотр GitHub релизов
gh release list --repo moseffect21/appmetrica-push-sdk
```
